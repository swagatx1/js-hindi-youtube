"use strict"; // treat all JS code as newer version


// alert(3+3)  //we are using node js not browser this can only run in brouser js

console.log(3
    +
     3
);  // code readeablity be high


let name = "swagat"     //string => ""
let age = 19            // int => fom -2^53 to +2^53 for bigger val use bigint
let inLoggedIn = false  // boolean => 0/1
let empty = null;       // null => standard representation of empty not 0
let state;              // undined => 
                        // bigint => to store bigger integer values
                        // symbol => unique mainly used in react we will learn it in react 

//objects

console.log(typeof age); // number
console.log(typeof null); // object
console.log(typeof state); // undefined
